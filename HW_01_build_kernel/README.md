
> [!info]
>  # **Примечания** 
>  - **config.working** - файл конфигурации с параметрами, настроенными в соответствии с заданием (перед запуском сборки ядра)
>  - **config-6.1.130** - финальный вариант файла конфигурации (из собранного ядра)
>  - Параметры **CONFIG_BPF_XXX** не стал выключать, так как планирую поэкспериментировать с данной подсистемой
>  - Параметр **RANDOMIZE_BASE** также не стал отключать, так как буду отключать его в настройках загрузчика (параметром **nokaslr** командной строки ядра в файле `/etc/default/grub`)
